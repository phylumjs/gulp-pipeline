{
	"name": "gulp-pipeline",
	"version": "3.0.0",
	"description": "Create a gulp task that runs a pipeline or pipeline task.",
	"main": "lib",
	"scripts": {
		"test": "nyc -- ava --verbose",
		"dev": "ava --watch --verbose",
		"report-coverage": "nyc report --reporter=text-lcov | coveralls"
	},
	"devDependencies": {
		"@phylum/pipeline": "^3.0.0",
		"ava": "^1.2.0",
		"coveralls": "^3.0.2",
		"gulp": "^4.0.0",
		"nyc": "^13.1.0"
	},
	"peerDependencies": {
		"@phylum/pipeline": "^3.0.0"
	},
	"repository": "github:phylumjs/gulp-pipeline",
	"author": "Max J. Polster",
	"license": "MIT"
}
